# Core runtime -----------------------------------------------------------------
numpy>=1.26
requests>=2.31
websockets>=12.0
python-dotenv>=1.0

# Reinforcement learning ---------------------------------------------------------
# torch and stable-baselines3 are heavy. The agent degrades to a rule-based
# policy when they are absent (see agent_system/rl/agent.py), so on a
# constrained machine you can install everything above this line and still run
# backtests, paper trading and the live loop.
gymnasium>=0.29
torch>=2.0
stable-baselines3>=2.2
tensorboard>=2.15

# Development ---------------------------------------------------------------------
pytest>=8.0
